apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "po-demo.fullname" . }}-test-connection"
  labels:
    {{- include "po-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "po-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
